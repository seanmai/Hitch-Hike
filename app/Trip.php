<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Trip extends Model
{
    protected $guarded = [
    ];

    // protected $fillable = [
    //     'title', 'driver_id', 'description', 'pickup', 'dropoff', 'available_seats', 'departure_time'
    // ];
}
